# The component of the hostname.  Must be suitable for the first parameter of
# urlparse.urljoin
HOST = 'http://localhost:8080/'

# The generator string.  Please leave as 'firmant' just to spread the word (or
# change it if you absolutely must).
GENERATOR = 'Firmant 0.10'

# Our enabled components
PLUGINS = ['firmant.frontend.txt',
           'firmant.frontend.jinja2',
           'firmant.frontend.jinja2.resolvers',
           'firmant.frontend.atom',
           'firmant.frontend.atom.resolvers',
           'firmant.plugins.filters.markdown',
           'firmant.plugins.datasource.flatfile.atom']
ENTRY_PERMALINK = 'firmant.frontend.jinja2.resolvers'
FEED_PERMALINK  = 'firmant.frontend.atom.resolvers'

FRONTEND_TXT_PREFIX = ''
FRONTEND_JINJA2_PREFIX = 'jinja2'
FRONTEND_JINJA2_ENCODING = 'utf-8'
FRONTEND_JINJA2_MIME = 'text/html'
FRONTEND_ATOM_PREFIX = 'atom'

import os
TEMPLATES = [os.path.join(os.getcwd(), 'templates')]

# Atom Default Feed ('/atom/')
ATOM_DEFAULT_TITLE = 'Firmant Atom Feed'
ATOM_DEFAULT_RIGHTS = 'Same as source.'
ATOM_DEFAULT_SUBTITLE = 'The default atom feed'

# Markdown options
MARKDOWN_XHTML_ENABLED = True
# Valid options: 'replace', 'remove', 'escape'
MARKDOWN_XHTML_SAFE_MODE = 'escape'

# Backend options
ATOM_PROVIDER = 'firmant.plugins.datasource.flatfile.atom'

# Flatfile options
FLATFILE_BASE = 'checkout/'
